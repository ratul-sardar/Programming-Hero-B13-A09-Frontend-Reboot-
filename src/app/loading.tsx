import LoadingSpinner from "@/components/shared/Loading Spinner/LoadingSpinner";

const Loading = () => {
  return (
    <section className="w-full flex items-center justify-center h-svh">
      <LoadingSpinner />
    </section>
  );
};

export default Loading;
